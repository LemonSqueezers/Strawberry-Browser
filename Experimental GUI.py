import requests

import sys

import tkinter as tk
tk.Tk(screenName=None,  baseName=None,  className='StrawberryGUI',  useTk=1);



win=tk.Tk() #creating the main window and storing the window object in 'win'
win.geometry('500x500') #setting the size of the window
entry = tk.Entry(fg="black", bg="white", width=50)
data = entry.get

